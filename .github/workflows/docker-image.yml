name: Build and Publish Image

on:
 push:
   branches: [ "main" ]
 pull_request:
   branches: [ "main" ]

jobs:

 build:

   runs-on: ubuntu-latest

   steps:
   - uses: actions/checkout@v4
   - name: Build the Docker image
     run: |
       cd "03 Task"
       ls -l
       echo "Starting build"
       docker build -f files/app/Dockerfile -t spring-image:latest
   - name: Publish image
     run: echo "Skipping" #docker build -t spring-image:latest -f files/app/Dockerfile .
