name: Build and Publish Image

on:
 push:
   branches: [ "main" ]
 pull_request:
   branches: [ "main" ]

jobs:

 build:

   runs-on: ubuntu-latest

   steps:
   - uses: actions/checkout@v4
   - name: Build the Docker image
     run: |
       cd "03 Task"
       ls -l
       docker build -t spring-image:latest -f files/app/Dockerfile
   - name: Publish image
     run: docker build -t spring-image:latest -f files/app/Dockerfile .
